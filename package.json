{
  "name": "@zimtsui/linked-list",
  "version": "0.0.1",
  "description": "",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "scripts": {
    "test": "tsc -b ./test/src/tsconfig.json && ava",
    "build": "tsc -b ./src/tsconfig.json",
    "clean": "rm -rf ./build",
    "docs": "typedoc --tsconfig ./src/tsconfig.json --excludePrivate --excludeProtected --cleanOutputDir ./src/index.ts"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/zimtsui/linked-list.git"
  },
  "author": "Zim",
  "dependencies": {
    "@types/assert": "^1.5.6",
    "assert": "^1.5.0"
  },
  "devDependencies": {
    "ava": "^4.3.0"
  }
}
